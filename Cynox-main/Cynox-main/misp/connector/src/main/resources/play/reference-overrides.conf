akka.actor {
  serializers {
    misp = "org.thp.cynox.connector.misp.services.MispSerializer"
  }

  serialization-bindings {
    "org.thp.cynox.connector.misp.services.MispMessage" = misp
  }
}
