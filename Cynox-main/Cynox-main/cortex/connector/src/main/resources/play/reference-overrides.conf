akka {
  actor {
    serializers {
      //cortex-schema-updater = "org.thp.cynox.connector.cortex.models.SchemaUpdaterSerializer"
      cortex-jobs = "org.thp.cynox.connector.cortex.services.CortexSerializer"
    }

    serialization-bindings {
      //"org.thp.cynox.connector.cortex.models.SchemaUpdaterMessage" = cortex-schema-updater
      "org.thp.cynox.connector.cortex.services.CortexActorMessage" = cortex-jobs
    }
  }
}
